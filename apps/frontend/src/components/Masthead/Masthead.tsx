import type { ReactElement } from 'react';
import styles from './styles.module.scss';

interface Props {
}

export function Masthead(props: Props): ReactElement {
    return (
        <div className={styles.masthead}>

        </div>
    )
}